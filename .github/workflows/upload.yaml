name: upload a file

on:
  push:
    branches:["master"]


jobs:
  upload:
    name: upload test
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v2
      -run: mkdir -p ./Uploaded Contents
      -run: echo helloworld ./Uploaded Contents/helloworld.txt

      - uses: actions/upload-artifact@v3
        with:
          name: artifactttt
          path: ./Uploaded Contents/helloworld.txt
